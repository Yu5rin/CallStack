# CallStack

電話の通話時間をキーボードショートカットで記録するWindows向けデスクトップアプリ。
会議の録音・文字起こしにも対応。

## 主な機能

- **グローバルショートカット** で通話の開始／終了／保留を記録（既定 `Ctrl+Shift+S` / `Ctrl+Shift+E` / `Ctrl+Shift+H`）
- **統合開始ダイアログ** — 「▶ 開始」から通話/会議をタブで選び、タイトル・連絡先を入れてから開始（種別は後から変更も可能）
- **会議の記録**（既定 `Ctrl+Shift+M` で即開始）— タイトル・参加者を記録し、通話とは分けて集計
- **データ保護** — 記録・文字起こし中のスリープ抑止、録音中の終了確認と保存待ち、未保存録音の起動時自動復元
- **柔軟な録音ソース**（MP3, 自動開始）— マイク／システム音声をチェックボックスで自由に組み合わせ。システム音声は**画面全体／特定ウィンドウ**を選択可（実験的）。開始時にソース選択ダイアログを表示（通話・会議で既定値を分けて設定可能）
- **録音の一時停止/再開**（既定 `Ctrl+Shift+P`、HUD・ヘッダーからも操作可）
- **マーカー（ブックマーク）** — 録音中に HUD・ヘッダーから打刻し、あとで該当位置へワンクリックでジャンプ。ラベル編集可
- **議事録 (Markdown) エクスポート** — タイトル・参加者・メモ・マーカー・文字起こしをまとめて保存
- **ローカル文字起こし**（whisper.cpp + 自動モデルダウンロード、完全オフライン）— **進捗表示・キャンセル**対応、高精度 **large-v3-turbo** モデルと**用語ヒント**（初期プロンプト）で固有名詞の認識を改善
- **ライブ文字起こし（実験的 / Vosk）** — 録音中の音声をその場で認識し、独立したライブ字幕ウィンドウにリアルタイム表示。ウィンドウは自由に移動・リサイズ、文字サイズ（小/中/大）変更、スクロールで全文確認・「最新へ」で追従復帰、× で非表示。録音終了後は whisper が高精度の確定版に置き換え（2段方式・完全オフライン）
- **録音・文字起こしのダウンロード** — MP3 を名前を付けて保存、文字起こしをテキスト保存（タイムスタンプ付き/なし）
- **音声ファイルの取り込み** — 既存の録音（mp3/wav/m4a など）を通話/会議の記録として登録し文字起こし（ドラッグ&ドロップ対応）
- **whisper.cpp 本体のアプリ内ダウンロード** — 設定画面のボタンだけで文字起こしのセットアップが完結
- **カスタマイズできる記録一覧** — 列のドラッグ並び替え・右クリックで表示/非表示・クリックで昇順/降順ソート・期間/録音有無の詳細フィルタ
- **文字起こしの修正** — セグメント単位でその場編集（誤認識をワンクリックで直せる）
- **ゴミ箱** — 削除した記録は30日間保持され、いつでも復元可能
- **統計の通話/会議タブ** — 会議は会議名別に集計
- **初回セットアップウィザード** と **更新通知**（起動時に新バージョンを確認。GitHub Releases の API に問い合わせるのみで、通話・録音データ等は一切送信しません）
- **自動更新（Windows限定）** — 「今すぐ更新」はいつでも使えます。設定で自動更新を ON にすると、新バージョンの検知時にダウンロード・SHA256検証・展開までバックグラウンドで済ませ、ボタン1つ（または通知のクリック）で再起動して適用できます（記録中は適用されません。詳細は [自動更新について](#自動更新について) を参照）
- **Windows ログイン時の自動起動**（設定でオン/オフ）
- **録音の安定性** — 録音は専用の不可視ウィンドウで実行され、メイン窓の最小化・復帰の影響を受けません
- **通話中の HUD** に経過時間・録音状態・リアルタイム**レベルメータ**を表示
- **累積記録** を JSON で永続化＋日次バックアップ
- **CSV エクスポート / インポート**（UTF-8 + BOM で Excel 対応、id で upsert）
- **通話時間サマリー**（合計／平均／最長／最短／今日・今週・今月）
- **統計ダッシュボード**（過去30日の棒グラフ、タグ別パイ、時間帯別ヒストグラム、連絡先別 TOP10）
- **連絡先別サマリー** — 連絡先クリックで一覧をその人で絞り込み
- **タグ／カテゴリ** とメモ、連絡先名・電話番号の事後編集
- **保留時間** の区間記録と純通話時間の自動算出
- **システムトレイ常駐** と **通話中の HUD ウィンドウ**（常時最前面、経過時間を1秒更新）
- **長電話アラート**（指定分経過で OS 通知）
- **音声フィードバック**（開始／終了時のビープ音）
- **録音の保管期限** 自動削除（既定 90 日、空欄で無制限）
- **検索／フィルター**（タグ・メモ・連絡先・電話番号・文字起こし全文＋未タグ抽出）
- **手動追加** で記録を後から作成も可能

## インストール

### 動作環境

- Windows 10 / 11（64ビット）
- インストーラは無く、ZIP を展開したフォルダからそのまま起動します（管理者権限は不要）

### 手順

1. [Releases](https://github.com/Yu5rin/CallStack/releases/latest) から `CallStack-<version>-win-x64.zip` をダウンロード
2. ZIP を好きなフォルダに展開
   - 自動更新を使う場合は、`Program Files` など書き込みに管理者権限が必要な場所を避けてください（[自動更新について](#自動更新について)）
3. 展開したフォルダの `CallStack.exe` を実行

設定と記録は `%APPDATA%\CallStack\` に保存されます（[データの保存場所](#データの保存場所)）。
文字起こしを使う場合は [文字起こしの準備](#文字起こしの準備) を参照してください。

### 更新

起動時に GitHub の最新リリースを確認し、新しい版があれば通知します（設定 → バージョン情報 から手動でも確認できます）。

- **自動更新を使う場合**: 設定で自動更新を ON にすると、ダウンロードから入れ替えまでアプリが行います。詳細は [自動更新について](#自動更新について)
- **手動で更新する場合**: CallStack を終了し（トレイに残っていればトレイのメニューから終了）、新しい版の ZIP を展開したフォルダで古いフォルダを置き換えます
  - 設定と記録は `%APPDATA%\CallStack\` にあるため、フォルダを置き換えても残ります
  - `resources\whisper\` に手で置いた whisper.cpp はフォルダと一緒に置き換わるので、新しいフォルダへ移し直してください（設定画面からダウンロードした whisper.cpp は `%APPDATA%\CallStack\` 側にあるため影響しません）

## 前提条件（ソースからビルドする場合）

| 項目 | 必要バージョン |
|------|---------------|
| OS   | Windows 10 / 11（`.exe` ビルドの場合は必須） |
| Node.js | **18 以上**（推奨 20 LTS 以降） |
| npm  | Node.js 同梱版でOK |
| Git  | リポジトリのクローンに使用 |

確認コマンド（PowerShell またはコマンドプロンプト）：

```powershell
node --version    # v20.x.x など
npm --version     # 10.x.x など
git --version
```

Node.js が未インストールなら https://nodejs.org/ja から LTS 版をインストールしてください。

## セットアップ手順（はじめての方向け）

> ⚠️ **すべてのコマンドは「CallStack フォルダの中」で実行します。**
> まず `cd` でこのプロジェクトのフォルダに移動してから npm コマンドを実行してください。

### 1. リポジトリをクローン（または既存フォルダへ移動）

PowerShell またはコマンドプロンプトを開き、好きな作業フォルダに移動してからクローン：

```powershell
# 例: ホーム配下に置く場合
cd $HOME
git clone https://github.com/Yu5rin/CallStack.git
cd CallStack
```

すでに `CallStack` フォルダがあるなら、そのフォルダに `cd` してください：

```powershell
cd C:\path\to\CallStack
```

ブランチが必要なら：

```powershell
git fetch origin
git checkout claude/call-tracking-app-aF1y4
```

### 2. 依存パッケージのインストール

**`package.json` があるフォルダ（= `CallStack` 直下）** で：

```powershell
npm install
```

- 5〜10 分ほどかかります（Electron バイナリのダウンロードを含むため）
- 完了後、`node_modules` フォルダが作成されます
- 社内プロキシ環境などで Electron のダウンロードに失敗する場合は、後述の「よくあるエラー」を参照

### 3. 開発モードで起動

```powershell
npm run dev
```

- Vite 開発サーバーが立ち上がり、自動的に Electron ウィンドウが開きます
- ホットリロード対応：UI を変更するとすぐ反映されます
- 終了するには、ターミナルで `Ctrl+C`、またはアプリのトレイメニューから「終了」

### 4. プロダクションビルド（型チェック＋アセット出力）

```powershell
npm run build
```

- 型チェック → `dist/`（UI）と `dist-electron/`（main・preload）が生成されます
- まだ `.exe` にはなりません（次のステップで作ります）

### 5. 配布用パッケージの作成

#### 方法A: ZIP 配布（既定・インストール不要）

```powershell
npm run dist:win
```

- 完了後、`release\<version>\CallStack-<version>-win-x64.zip` が出力されます
- 配布された ZIP を解凍 → `CallStack.exe` をダブルクリックで起動できます（インストール不要）
- 設定や記録は通常どおり `%APPDATA%\CallStack\` に保存されます

#### 方法B: ポータブル `.exe` 1ファイル

```powershell
npm run dist:portable
```

- `release\<version>\` に単一の `.exe` ファイルが出力されます
- USB メモリで持ち運ぶ用途などに便利

#### 方法C: インストーラ（NSIS）

```powershell
npm run dist:installer
```

- `release\<version>\CallStack Setup <version>.exe` が出力されます
- インストール先を指定するインストーラ形式

---

## キーボードショートカット（既定）

| 操作 | キー |
|------|------|
| 通話開始 | `Ctrl+Shift+S` |
| 会議開始 | `Ctrl+Shift+M` |
| 記録終了 | `Ctrl+Shift+E` |
| 保留トグル | `Ctrl+Shift+H` |
| 録音の一時停止/再開 | `Ctrl+Shift+P` |
| マーカーを打つ | `Ctrl+Shift+K` |
| タグ1〜4 を付け外し | `Ctrl+Shift+1` 〜 `Ctrl+Shift+4` |
| メイン窓を表示/隠す | `Ctrl+Shift+T` |
| 設定を開く | `Ctrl+Shift+,` |

アプリの「設定」ページで自由に変更できます。

## データの保存場所

| 内容 | パス |
|------|------|
| 設定・通話記録 | `%APPDATA%\CallStack\data.json` |
| 自動バックアップ | `%APPDATA%\CallStack\backups\data-YYYYMMDD.json`（最新30日分） |
| 録音ファイル | `%APPDATA%\CallStack\recordings\{id}.mp3` |
| Whisper モデル | `%APPDATA%\CallStack\models\ggml-{size}.bin` |

エクスプローラーのアドレスバーに `%APPDATA%\CallStack` と入れれば直接開けます。

## 録音について

設定画面の「録音」セクションで有効化します（**既定 OFF**、有効化時に同意確認モーダルが表示されます）。

- **形式**: MP3（ffmpeg 内蔵）。ビットレートは 64/96/128/192 kbps から選択
- **音声ソース**: マイクのみ／マイク+システム音声（PC スピーカ出力）
- **自動文字起こし**: ON にすると録音終了後に自動でテキスト化
- **保管期限**: 既定 90 日。期限切れの音声ファイルのみ削除（通話記録は残ります）

> ⚠️ 通話の録音には**相手の同意が必要**な場合があります。地域・業務上のルールを確認してから利用してください。
> 録音は端末内のみに保存され、外部に送信されません（文字起こしもオフラインで実行）。

## 文字起こしの準備

文字起こしは **whisper.cpp** をローカルで実行します。初回セットアップで実行ファイルが必要です。

### 1. whisper.cpp の実行ファイルを取得

**設定 → 文字起こし** の「whisper.cpp をダウンロード」ボタンを押すと、Windows 用の実行ファイルを取得して自動で配置します（`%APPDATA%\CallStack\whisper\`。手で置いたものより優先して使われます）。

手で配置する場合は次のとおりです。

1. https://github.com/ggerganov/whisper.cpp/releases から最新の Windows ビルドをダウンロード
   - 例: `whisper-blas-bin-x64.zip`（CPU 高速化版、推奨）
2. zip を解凍し、**中のファイルをすべて**（`whisper.dll`・`ggml*.dll` などの DLL も含めて）以下のフォルダへコピー。exe だけでは DLL が見つからず `0xC0000135` で失敗します
   - 開発時: `resources\whisper\`
   - 配布された .zip 版: `<解凍フォルダ>\resources\whisper\`
3. 実行ファイルの名前が `main.exe`（古い版）なら **`whisper-cli.exe`** に変更

> アプリ内ダウンロード・`resources\whisper\` のどちらにも実行ファイルが無いときは、環境変数 `TELTIMESTACK_WHISPER_BIN` に設定した絶対パスが使われます（開発用）。

### 2. モデルファイルのダウンロード

アプリの **設定 → 文字起こし** セクションで使いたいモデルの「ダウンロード」ボタンを押します。

| モデル | サイズ | 用途 |
|--------|--------|------|
| tiny   | 約 75 MB  | 速度優先 |
| base   | 約 142 MB | 軽量 |
| **small**  | **約 466 MB** | **バランス・推奨（日本語実用レベル）** |
| **Kotoba-Whisper v2.0** | 約 1.5 GB | **日本語特化・高速（distil 系）。日本語会議におすすめ** |
| medium | 約 1.5 GB | 高精度（CPU でも動作可、時間はかかる） |
| large-v3-turbo | 約 1.6 GB | 最高精度・medium より高速 |

> 高速化のヒント: 文字起こしは自動で CPU コア数ぶんのスレッドを使います。さらに速くしたい場合は
> **Kotoba-Whisper v2.0**（日本語特化で high-speed）や **GPU (CUDA) 版**（設定の隠しコマンドで解放）が有効です。

ダウンロードしたモデルは `%APPDATA%\CallStack\models\` に保存されます。

### 3. 動作確認

録音した通話の編集ダイアログを開き、「文字起こし」ボタンを押すと処理が始まります。
完了するとセグメント表示され、クリックでその位置から再生できます。

## ライブ文字起こし（実験的）

録音中の音声を **Vosk** でその場で認識し、暫定テキストを表示します。

- **HUD** — 認識された最新の1行が HUD の下に表示されます
- **編集画面** — 録音中に編集ダイアログを開くと、認識結果がタイムスタンプ付きで流れます
- 録音終了後は従来どおり **whisper が確定版を生成**し、暫定テキストを置き換えます
  （whisper 未セットアップの場合は暫定テキストがそのまま残ります）

### セットアップ

1. **設定 → 録音** で録音を有効にする
2. **設定 → 文字起こし → ライブ文字起こし（実験的 / Vosk）** で
   「エンジンをダウンロード (約7MB)」と「モデルをダウンロード」を押す
   - `small-ja`（約 48MB・軽量、推奨）/ `ja`（約 1GB・高精度）
3. 「録音中にライブ文字起こしを表示する」にチェック

> エンジン・モデルは `%APPDATA%\CallStack\vosk\` / `vosk-models\` に配置され、
> 認識はすべて端末内で実行されます（外部送信なし）。

### 専門用語の精度を上げる

- **whisper（確定版）**: 設定 → 文字起こし →「用語ヒント」に社名・人名・専門用語を読点区切りで登録すると、固有名詞の認識精度が上がります。
- **ライブ（Vosk）/ whisper 共通**: 設定 →「単語登録（置換辞書）」で「誤り → 正しい語」を登録すると、認識結果を自動で置換して表記を補正します（例: `コールスタック → CallStack`）。ライブで専門用語が別の語に誤認識されるときに有効です。より高精度が必要な場合はライブのモデルを `ja`（約1GB）に切り替えてください。

## 自動更新について

CallStack は起動時（既定 ON。設定でオフにできます）に GitHub の最新リリースを確認し、新しい版があれば通知します。通話・録音・文字起こしなどのデータは一切送信されません。送信するのは github.com / api.github.com への HTTP の GET リクエストだけです。

### 確認の仕組み（会社のネットワークでも更新できるように）

GitHub の API（`releases/latest`）には**1時間60回（未認証）の上限があり、これは端末ごとではなく IP アドレスごとに数えられます**。会社のように多数の PC が同じ出口 IP を共有する環境では、他の通信で先に使い切られていて常に上限に達し、更新の確認そのものができないことがあります。

これを避けるため、CallStack はまず GitHub の Atom フィード（`releases.atom`。API の回数上限とは別枠）で「新しい版があるか」だけを確認し、**新しい版があるときだけ** API を呼んでダウンロード URL・SHA256 を取りに行きます。それでも API が上限で失敗した場合は、ダウンロード URL を規則から組み立てて更新を続行します（この場合のみ SHA256 の照合は省かれます。設定画面にその旨が表示されます）。

それでも自動更新がうまくいかないときは、設定 → バージョン情報 →「うまく更新できないとき」の「通信を確かめる」で、配布物の置き場まで実際に通信が届くかを確認できます（ファイルは保存しません）。詳しい内訳はログフォルダの `app.log` に残ります。

なお、**v2.7.x 以前の CallStack は API に直接問い合わせる作りのため、同じ問題（会社などの共有回線では一度も自動更新できない）が起こりえます。** その場合は、お手数ですが一度だけ [Releases](https://github.com/Yu5rin/CallStack/releases/latest) から手動でダウンロードしてください。それ以降はこの版が自動更新を引き継ぎます。

### 「今すぐ更新」（いつでも使える）

設定 → バージョン情報 →「今すぐ更新」を押すと、その場で次を行います。

1. GitHub Releases から Windows 用 ZIP をダウンロード
2. 配布元が SHA256（digest）を提供していれば検証（提供されない場合は照合を省いて続行し、その旨を表示します）
3. アプリのインストール先フォルダの隣に展開して準備完了

ここまでではファイルは入れ替わりません。「v○○ を適用して再起動」を押したとき、または通知をクリックしたときだけ、実際に入れ替えて再起動します。記録が進行中の場合は適用を拒否します。

### 自動更新（Windows限定・既定OFF）

設定 →「新しい版を見つけたら、ダウンロードまで自動で済ませておく」を ON にすると、起動時・常駐中（前回の確認から24時間おき）の確認で新しい版を見つけたとき、上の 1〜3 をバックグラウンドで先に済ませておきます。OFF でも「今すぐ更新」自体は変わらず使えます——このトグルが変えるのは、通知が来た時点でダウンロードが終わっているかどうかだけです。

### ファイルの入れ替え方法

実行中の exe を直接上書きできない Windows の制約に対応するため、以下の手順で安全に入れ替えます（Chrome・VS Code 等と同様の方式）。

1. アプリを終了
2. 現在のフォルダを `.old` にリネーム
3. 新バージョンのフォルダを元の場所へ移動
4. 新しい exe を起動
5. 途中で失敗した場合は自動的に元の状態へロールバック（`.old` を復元して起動）
6. 入れ替えが最後まで終わった合図として、新しいフォルダに目印ファイルを書く
7. 前回の更新で残った `.old` フォルダは、次回起動時にこの目印を確認したうえで自動で削除（目印が無い＝入れ替えが完了しないまま終わった可能性がある場合は、復旧の材料として残します）

### SHA256 検証が防ぐもの・防げないもの

SHA256 の照合で検出できるのは**転送中の破損**です。ハッシュ値自体も同じ GitHub API・同じリリースから取得しているため、GitHub 上の配布元（リポジトリ・アカウント）そのものが差し替えられた場合はハッシュ値も一緒に差し替わっており、検出できません。ファイルへの署名（コード署名）による検証も行っていません。

**制約・注意事項**:
- 現在は Windows の ZIP 配布形式のみ対応（NSIS インストーラ版・ポータブル版では自己更新は動作しません）
- `Program Files` など書き込み権限が無い場所に配置している場合は、自動更新は行われず、従来どおり手動でのダウンロードが必要です（事前チェックで検知し、安全側に倒します）
- ソースからビルドした場合（`npm run dev` の開発モード）では自動更新は無効化されます

## よくあるエラーと対処

### `'npm' は内部コマンドまたは外部コマンド…として認識されていません`
Node.js がインストールされていません。https://nodejs.org/ja から LTS 版をインストールし、ターミナルを開き直してください。

### `npm ERR! code ENOENT` / `package.json not found`
`package.json` のないフォルダで実行しています。`cd C:\path\to\CallStack` でプロジェクトフォルダに移動してから再実行してください。`dir` で `package.json` が見えることを確認しましょう。

### `npm install` の途中で `Electron failed to install correctly` / ダウンロード失敗
社内ネットワーク・プロキシで Electron バイナリの取得に失敗するケース。以下のいずれかを試してください：

```powershell
# プロキシを設定
npm config set proxy http://your.proxy:port
npm config set https-proxy http://your.proxy:port

# Electron のミラーを指定（必要に応じて）
$env:ELECTRON_MIRROR="https://npmmirror.com/mirrors/electron/"

# 再インストール
Remove-Item -Recurse -Force node_modules, package-lock.json
npm install
```

### `npm run dev` を打ったが何も起きない／ウィンドウが出ない
- ターミナルのログに `VITE v5...` が出るまで数秒待ってください
- Windows Defender が初回ブロックすることがあります。許可してください
- それでも出ない場合はログをコピーして共有してください

### `npm run dist:win` で「コード署名がありません」「サポートされていないプラットフォーム」など
- `.exe` ビルドは **Windows 上での実行が必須** です（macOS/Linux からはできません）
- 署名警告は通常運用上問題ありません（ローカル利用なら無視可）

### `Error: Cannot find module 'electron'`
`npm install` が完了していません。プロジェクトフォルダで `npm install` を再実行してください。

---

## スクリプト一覧

| コマンド | 用途 |
|----------|------|
| `npm install` | 依存をインストール（初回のみ／`package.json` 変更時） |
| `npm run dev` | 開発起動（ホットリロードあり） |
| `npm run typecheck` | TypeScript 型チェックのみ |
| `npm run build` | プロダクションビルド（`dist/` と `dist-electron/`） |
| `npm run dist:win` | Windows 用 **ZIP** を `release/` に出力（既定・インストール不要） |
| `npm run dist:portable` | 単一の **ポータブル `.exe`** を出力 |
| `npm run dist:installer` | **NSIS インストーラ**（`Setup .exe`）を出力 |

## 技術スタック

Electron 33 + React 18 + TypeScript 5 + Vite 5 + Tailwind CSS 3 + Recharts + dayjs + nanoid

## ライセンス

MIT License — 詳細は [LICENSE](./LICENSE) を参照してください。

依存ライブラリ（React・Electron・ffmpeg 等）はそれぞれのライセンスに従います。
whisper.cpp・Vosk はアプリが初回セットアップ時に別途ダウンロードする外部バイナリで、
このリポジトリには同梱していません。
